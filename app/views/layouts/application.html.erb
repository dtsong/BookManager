<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BookManager :: <%= controller.controller_name.capitalize %> - <%= controller.action_name.capitalize %></title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <nav>
    <div class="nav-wrapper container">
      <%= link_to "BookManager", home_path,
            { :class=> "brand-logo left" } %>
      <ul class="right">
        <li><%= link_to "Categories", categories_path %></li>
        <li><a class="dropdown-button" href="#!" data-activates="dropdown1">Authors<i class="mdi-navigation-arrow-drop-down right"></i></a></li>
        <ul id="dropdown1" class="dropdown-content">
          <li><%= link_to "List Authors", authors_path %></li>
          <li><%= link_to "Add an Author", new_author_path %></li>
        </ul>
      </ul>
    </div>
  </nav>
    <div class="container">
      <div class="row">
        <div class="col s8">
          <% flash.each do |name, msg| %>
    <div class="alert-box <%= name == :notice ? "success" : "warning" %> radius">
      <a href="#" class="close">&times;</a>
      <%= msg %>
    </div>
  <% end %>
          <!-- MAIN CONTENT GOES HERE -->
          <%= yield %>
          <%= javascript_include_tag "application" %>
        </div>

        <br><br><br>

        <div class="col s3 offset-s1 card">
          <!-- SIDEBAR CONTENT GOES HERE -->
          <%= render partial: 'partials/quick_links' %>
        </div>
      </div>
    </div>

    <footer class="container">
      <div class="row">
        <div class="col s12 center">
          <%= render partial: 'partials/footer' %>
        </div>
      </div> 
    </footer>     
  </body>
</html>
